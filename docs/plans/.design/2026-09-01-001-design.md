---
type: design
design-id: 2026-09-01-001-design
scope-id: 2026-09-01-001-scope
research-id: 2026-09-01-001-research
status: complete
interactionMode: smart
complexity: low
tier_recommended: shallow
approach: >-
  Rewrite end-session/SKILL.md to align with Arreio conventions. Add
  interactionMode selection, remove version-bump detection, add artifact
  persistence, implement session-based agent attribution, and create a
  lightweight error-handling reference. Update arreio-init to create the
  end-session artifact directory.
high-level-technical-design: >-
  The skill becomes a three-phase workflow: Pre-flight → Prepare Commit →
  Create Commit → Post-Commit. Each phase checks interactionMode before
  proceeding. A session artifact captures the commit message, changed files,
  and attribution. The post-commit step suggests /learn maintain instead of
  chaining /update-learnings.
implementation-units:
  - id: TASK-01
    name: Add interactionMode support and frontmatter alignment
    acceptance-criterion: >-
      end-session/SKILL.md frontmatter includes disable-model-invocation,
      version, and timestamp. An Interaction Method section offers
      Detailed/Smart/Autopilot and stores interactionMode in a context object.
  - id: TASK-02
    name: Remove version-bump detection and changelog logic
    acceptance-criterion: >-
      Phase 3.5 (Version & Changelog Check) is entirely removed from
      end-session/SKILL.md. No references to CHANGELOG.md, version-bearing
      files, or git tags remain.
  - id: TASK-03
    name: Add session artifact persistence
    acceptance-criterion: >-
      A new Phase 4 (Save Session Artifact) writes a markdown file to
      docs/plans/.end-session/<session-id>.md with commit subject, body,
      changed files, and timestamp. The session-id uses YYYY-MM-DD-NNN format.
  - id: TASK-04
    name: Implement session-based agent attribution
    acceptance-criterion: >-
      Agent attribution is derived from file changes in the current session
      (git diff or equivalent). If all changed files were last modified by the
      same agent (detected via heuristics or git notes), that agent name is
      used. Otherwise fall back to the current agent name. The [AGENT: ...]
      trailer remains mandatory.
  - id: TASK-05
    name: Add error-handling reference and tool-agnostic rewrite
    acceptance-criterion: >-
      skills/end-session/references/error-handling.md exists with structured
      error categories (Missing Changes, Invalid Input, File System, Git
      Errors), retry limits, and recovery workflow. All git CLI commands in
      SKILL.md are replaced with capability descriptions.
  - id: TASK-06
    name: Update arreio-init for end-session directories
    acceptance-criterion: >-
      arreio-init/SKILL.md Step 8 (or new step) creates
      docs/plans/.end-session/ directory. Success Criteria list includes the
      new directory.
alternative-approaches:
  - name: Keep version-bump detection as optional flag
    rejected: User explicitly requested removal.
  - name: Use docs/archives/ for session artifacts
    rejected: Consistency with other skills using docs/plans/.<skill>/.
---

# Design: Fix end-session Skill Gaps

## Complexity

Low. Changes are within a single skill file and one reference file, plus a minor arreio-init update.

## Implementation Units

1. **TASK-01**: Frontmatter + interactionMode
2. **TASK-02**: Remove version-bump logic
3. **TASK-03**: Session artifact phase
4. **TASK-04**: Session-based agent attribution
5. **TASK-05**: Error-handling reference + tool-agnostic rewrite
6. **TASK-06**: arreio-init directory creation
